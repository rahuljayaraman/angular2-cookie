import { NgModule }                  from '@angular/core';
import { ANGULAR2_COOKIE_PROVIDERS } from './core';

@NgModule({
  providers: [ ANGULAR2_COOKIE_PROVIDERS ]
})
export class CookieModule { }
